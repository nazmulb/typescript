enum Direction {
    Up = 1,
    Down,
    Left,
    Right,
}

console.log(Direction.Up);

enum Responses {
    No,
    Yes,
}

console.log(Responses.No);

// String enums

enum Directions {
    Up = "UP",
    Down = "DOWN",
    Left = "LEFT",
    Right = "RIGHT",
}

console.log(Directions.Up);